# Machine-local overrides (gitignored). Merge priority: config/browser.local.toml > config/browser.toml
# Example below mirrors taobao-mcp Windows config.local.toml and uses the installed Edge browser.

[browser]
executable_path = 'C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe'

# If the machine has Chrome instead, leave executable_path empty and use the committed
# channel = "chrome" default. user_data_dir stays "./user_data/onshape_profile" so the
# Onshape login profile is independent of taobao-mcp.
